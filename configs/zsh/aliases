# source zshrc
alias srz="source $ZDOTDIR/.zshrc"

# TMUX startup options
alias tmux="tmux -f $XDG_CONFIG_HOME/tmux/.tmux.conf"

# Remap vi & vim to nvim (use `\vi` and `\vim` to disable)
alias vi="nvim"
alias vim="nvim"
